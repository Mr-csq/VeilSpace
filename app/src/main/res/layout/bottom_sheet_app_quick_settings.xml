<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/sheet_content"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_quick_settings_sheet"
    android:orientation="vertical"
    android:paddingStart="20dp"
    android:paddingTop="12dp"
    android:paddingEnd="20dp"
    android:paddingBottom="28dp">

    <View
        android:layout_width="38dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="20dp"
        android:background="@drawable/bg_quick_settings_handle" />

    <TextView
        style="@style/TextAppearance.PrivacySpace.Eyebrow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="APP QUICK SETTINGS" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/iv_app_icon"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:contentDescription="应用图标"
            android:scaleType="fitCenter" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_app_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/space_text_primary"
                android:textSize="21sp"
                android:textStyle="bold"
                tools:text="应用名称" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="长按并移动图标可继续排序"
                android:textColor="@color/space_text_secondary"
                android:textSize="12sp" />
        </LinearLayout>
    </LinearLayout>

    <TextView
        style="@style/TextAppearance.PrivacySpace.SectionTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="9dp"
        android:text="快捷设置" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_glass_panel"
        android:orientation="vertical"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp">

        <com.google.android.material.materialswitch.MaterialSwitch
            android:id="@+id/switch_show"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="在首页显示"
            android:textColor="@color/space_text_primary"
            android:textSize="15sp"
            android:textStyle="bold"
            app:thumbTint="@color/space_switch_thumb"
            app:trackTint="@color/space_switch_track" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingEnd="64dp"
            android:paddingBottom="14dp"
            android:text="关闭后应用将直接从隐藏空间首页移除。"
            android:textColor="@color/space_text_secondary"
            android:textSize="12sp" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/space_divider" />

        <com.google.android.material.materialswitch.MaterialSwitch
            android:id="@+id/switch_keep_alive"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="允许后台运行"
            android:textColor="@color/space_text_primary"
            android:textSize="15sp"
            android:textStyle="bold"
            app:thumbTint="@color/space_switch_thumb"
            app:trackTint="@color/space_switch_track" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingEnd="64dp"
            android:paddingBottom="14dp"
            android:text="沿用隐藏空间策略；关闭后应用退出前台时会自动隐藏。"
            android:textColor="@color/space_text_secondary"
            android:textSize="12sp" />
    </LinearLayout>
</LinearLayout>
