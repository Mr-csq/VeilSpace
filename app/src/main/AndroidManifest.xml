<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Work Profile 相关权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Device Admin 权限 -->
    <uses-permission android:name="android.permission.BIND_DEVICE_ADMIN" />
    <uses-permission android:name="android.permission.MANAGE_USERS" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.INTERACT_ACROSS_PROFILES" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />

    <application
        android:name=".PrivacySpaceApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_app_icon"
        android:crossProfile="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.PrivacySpace"
        tools:targetApi="31">
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- MainActivity 不显示在桌面 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PrivacySpace"
            android:icon="@drawable/ic_app_icon"
            android:roundIcon="@drawable/ic_app_icon">
        </activity>

        <activity
            android:name=".ui.files.ImagePreviewActivity"
            android:exported="false"
            android:theme="@style/Theme.PrivacySpace" />

        <!-- DisguiseActivity 伪装入口 -->
        <activity
            android:name=".ui.disguise.DisguiseActivity"
            android:exported="false"
            android:theme="@style/Theme.DisguiseWait"
            android:excludeFromRecents="true" />
        <!-- 主空间代理：由工作资料跨资料调用，用于打开真正的小米游戏中心 -->
        <activity
            android:name=".ui.disguise.GameCenterProxyActivity"
            android:exported="true"
            android:theme="@style/Theme.Transparent"
            android:excludeFromRecents="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="com.system.launcher.tools.action.OPEN_REAL_GAME_CENTER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>


        <receiver
            android:name=".ui.disguise.GameCenterProxyReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.system.launcher.tools.action.OPEN_REAL_GAME_CENTER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>
        <!-- Activity Alias 伪装成游戏中心 -->
        <activity-alias
            android:name=".GameCenterAlias"
            android:targetActivity=".ui.disguise.DisguiseActivity"
            android:exported="true"
            android:enabled="false"
            android:icon="@drawable/ic_app_icon"
            android:roundIcon="@drawable/ic_app_icon"
            android:label="游戏中心">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>


        <!-- Work Profile 桌面入口：伪装成游戏中心，手势通过后进入隐藏空间 -->
        <activity-alias
            android:name=".WorkProfileGameCenterAlias"
            android:targetActivity=".ui.disguise.DisguiseActivity"
            android:exported="true"
            android:enabled="true"
            android:icon="@drawable/ic_app_icon"
            android:roundIcon="@drawable/ic_app_icon"
            android:label="游戏中心">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- Work Profile 非桌面入口：用于跨资料 intent filter，不出现在桌面 -->
        <activity-alias
            android:name=".PrivacyActionAlias"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:enabled="false"
            android:icon="@drawable/ic_app_icon"
            android:roundIcon="@drawable/ic_app_icon"
            android:label="游戏中心">
            <intent-filter>
                <action android:name="com.system.launcher.tools.action.OPEN_PRIVACY_SPACE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity-alias>

        <receiver
            android:name=".work.WorkProfilePackageReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_CHANGED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".work.WorkProfileSetupReceiver"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <!-- DeviceAdminReceiver for Work Profile -->
        <receiver
            android:name=".work.WorkProfileAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.PROFILE_PROVISIONING_COMPLETE" />
            </intent-filter>
        </receiver>

    </application>

</manifest>







